# Providers

## CSPProvider

- Provides a `nonce` for inline `<style>` and `<script>` tags.
- Can disable inline style tags when strict CSP environments require it.

## DirectionProvider

- Sets text direction (`ltr`/`rtl`) context for mirrored components.
- Apply near app root where RTL toggles are user configurable.
