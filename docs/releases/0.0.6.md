# Release 0.0.6 🧾

- Date: 2026-02-28
- Channel: stable
- npm dist-tag: latest

## Summary ✅

- Lockstep patch release to `0.0.6` for all workspace packages.
- Fixed `CopyButton` clipboard-status behavior in `@kuzenbo/core` to match the current `useClipboard` contract.
- Fixed hooks showcase import in website runtime (`useFullscreen` -> `useFullscreenElement`) to restore type safety.
- Added missing `@testing-library/user-event` dev dependency in `@kuzenbo/hooks` for boundary compliance.

## Breaking Changes ⚠️

- None.

## Package Impact 📦

- All `packages/*` bumped to `0.0.6` in lockstep.
- Publish allowlist validated in dry-run:
  - `@kuzenbo/core`
  - `@kuzenbo/hooks`
  - `@kuzenbo/charts`
  - `@kuzenbo/notifications`
  - `@kuzenbo/styles`
  - `@kuzenbo/theme`

## Migration Notes 🛠️

- No migration required (patch release).

## Validation Evidence 🔍

- Local gates passed:
  - `bun run format`
  - `bun run lint`
  - `bun run typecheck`
  - `bun run test`
  - `bun run boundaries`
  - `bun audit`
- Release preflight passed:
  - `bun run release:validate -- --version 0.0.6 --channel stable --ref main`
  - `bun run release:dry-run -- --version 0.0.6 --channel stable --ref main`
- CI run URL: pending workflow dispatch
- npm packages page: pending publish
- Git tag: pending publish
