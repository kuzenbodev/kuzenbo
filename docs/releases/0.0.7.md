# Release 0.0.7 🧾

- Date: 2026-03-01
- Channel: stable
- npm dist-tag: latest

## Summary ✅

- Stable patch release for the Kuzenbo workspace.
- Lockstep version bump from `0.0.6` to `0.0.7`.
- No API-breaking changes introduced in this release.

## Breaking Changes ⚠️

- None.

## Package Impact 📦

- Published (allowlist): `@kuzenbo/core`, `@kuzenbo/hooks`, `@kuzenbo/charts`, `@kuzenbo/notifications`, `@kuzenbo/styles`, `@kuzenbo/theme`.
- Internal/private version bump only: `@kuzenbo/ai`, `@kuzenbo/code`, `@kuzenbo/datatable`, `@kuzenbo/date`, `@kuzenbo/cli`, `@kuzenbo/mcp`, `@kuzenbo/tiptap`, `@kuzenbo/storybook`, `@kuzenbo/website`.

## Migration Notes 🛠️

- No migration steps required for this patch release.

## Validation Evidence 🔍

- Local quality gates: `bun run format`, `bun run lint`, `bun run typecheck`, `bun run test`, `bun run boundaries`, `bun audit`.
- Release checks: `bun run release:validate -- --version 0.0.7 --channel stable --ref main`, `bun run release:dry-run -- --version 0.0.7 --channel stable --ref main`.
- CI run URL: pending workflow dispatch after commit/push approval.
- npm packages page: pending publish workflow completion.
- Git tag: pending publish workflow completion.
