# Release 0.0.4 🧾

- Date: 2026-02-26
- Channel: stable
- npm dist-tag: latest

## Summary ✅

- Stabilized React client-boundary behavior for library consumers by adding
  missing `"use client"` directives on client-facing entrypoints.
- Ensures client-only exports (for example provider/hook/component entrypoints)
  keep correct behavior when consumed from external Next.js apps.

## Breaking Changes ⚠️

- None.

## Package Impact 📦

- @kuzenbo/core:
  - Added missing `"use client"` directives on client-facing entrypoints,
    including provider/size surfaces and related UI entry modules.
- @kuzenbo/hooks:
  - Added `"use client"` directives to exported hook entrypoints.
- @kuzenbo/charts:
  - Version bumped in lockstep release train.
- @kuzenbo/notifications:
  - Version bumped in lockstep release train.
- @kuzenbo/date:
  - Version bumped in lockstep release train.
- @kuzenbo/theme:
  - Version bumped in lockstep release train.
- @kuzenbo/styles:
  - Version bumped in lockstep release train.
- @kuzenbo/ai:
  - Added `"use client"` directive to exported hook entrypoint.
- @kuzenbo/datatable:
  - Added `"use client"` directive to exported hook entrypoint.

## Migration Notes 🛠️

- No breaking migration required.
- If you previously added local wrapper workarounds to force client boundaries
  for Kuzenbo provider/hooks, you can now simplify and import directly from the
  published package surfaces in `0.0.4`.

## Validation Evidence 🔍

- CI run URL: (to be filled after workflow dispatch)
- npm packages page: https://www.npmjs.com/search?q=%40kuzenbo
- Git tag: v0.0.4
